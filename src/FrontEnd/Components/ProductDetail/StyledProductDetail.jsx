// 
import { styled } from "@mui/material/styles";
import {Grid} from "@mui/material";

const StyledProductDetail = styled(Grid)(() => ({
    padding:50,
    background:'rgba(255,255,255, 1)',
}));

export default StyledProductDetail;
